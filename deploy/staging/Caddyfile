{
    email {$FILMIQOO_TLS_EMAIL}
}

{$FILMIQOO_DOMAIN} {
    encode zstd gzip
    reverse_proxy api:8080

    header {
        -Server
        X-Content-Type-Options "nosniff"
        Referrer-Policy "no-referrer"
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
    }

    log {
        output stdout
        format json
    }
}
